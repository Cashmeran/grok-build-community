diff a/crates/codegen/xai-grok-shared/src/clipboard.rs b/crates/codegen/xai-grok-shared/src/clipboard.rs	(rejected hunks)
@@ -570,6 +570,7 @@ mod attachments_protocol {
 // ---------------------------------------------------------------------------
 #[cfg(target_os = "macos")]
 mod platform {
+    #[allow(unused_imports)]
     use std::process::{Command, Stdio};
     use std::sync::OnceLock;
 
@@ -1192,9 +1193,11 @@ mod platform {
 // ---------------------------------------------------------------------------
 #[cfg(not(target_os = "macos"))]
 mod platform {
-    use super::ImageData;
+    #[allow(unused_imports)]
     use std::process::{Command, Stdio};
 
+    use super::ImageData;
+
     /// No subprocess-free pasteboard probe exists off-macOS.
     pub(super) fn clipboard_image_snapshot() -> (Option<u64>, bool) {
         (None, false)
@@ -1983,6 +1986,7 @@ mod platform {
     // -- Public API ----------------------------------------------------------
 
     pub fn get_text() -> anyhow::Result<Option<String>> {
+        #[allow(unused_assignments)]
         let mut arboard_error = None;
         match arboard_get_text() {
             Ok(Some(text)) => return Ok(Some(text)),
@@ -2096,6 +2100,7 @@ mod platform {
     }
 
     pub fn get_image() -> anyhow::Result<Option<ImageData>> {
+        #[allow(unused_assignments)]
         let mut arboard_error = None;
         match arboard_get_image() {
             Ok(Some(image)) => return Ok(Some(image)),
