diff a/crates/codegen/xai-grok-shell/src/upload/gcs.rs b/crates/codegen/xai-grok-shell/src/upload/gcs.rs	(rejected hunks)
@@ -154,7 +154,9 @@ pub(crate) async fn upload_to_auth_diagnostics(
             );
         }
         Err(e) => {
-            tracing::warn!(error = %e, "failed to upload diagnostic log to auth-diagnostics");
+            tracing::warn!(
+                error = % e, "failed to upload diagnostic log to auth-diagnostics"
+            );
         }
     }
 }
