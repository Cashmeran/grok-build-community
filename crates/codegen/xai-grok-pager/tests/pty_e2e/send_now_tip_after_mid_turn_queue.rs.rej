diff a/crates/codegen/xai-grok-pager/tests/pty_e2e/send_now_tip_after_mid_turn_queue.rs b/crates/codegen/xai-grok-pager/tests/pty_e2e/send_now_tip_after_mid_turn_queue.rs	(rejected hunks)
@@ -16,16 +16,10 @@ async fn send_now_tip_after_mid_turn_queue() {
     );
 
     let binary = pager_binary().expect("resolve pager binary");
-    let env_refs = CONTEXTUAL_HINTS_ENV;
-    let mut harness = PtyHarness::spawn_with_content_env(
-        &binary,
-        DEFAULT_ROWS,
-        DEFAULT_COLS,
-        &content,
-        &[],
-        env_refs,
-    )
-    .expect("spawn pager with contextual hints");
+    let env = contextual_hints_env(&content);
+    let env_refs: Vec<(&str, &str)> = env.iter().map(|(k, v)| (k.as_str(), v.as_str())).collect();
+    let mut harness = PtyHarness::new(&binary, DEFAULT_ROWS, DEFAULT_COLS, &[], &env_refs)
+        .expect("spawn pager with contextual hints");
 
     harness
         .wait_for_text(WELCOME_SCREEN_SENTINEL, WELCOME_TIMEOUT)
