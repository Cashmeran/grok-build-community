diff a/crates/codegen/xai-grok-pager/tests/pty_e2e/agent_type_mismatch_no_keeps_current_session.rs b/crates/codegen/xai-grok-pager/tests/pty_e2e/agent_type_mismatch_no_keeps_current_session.rs	(rejected hunks)
@@ -58,7 +58,7 @@ async fn agent_type_mismatch_no_keeps_current_session() {
         harness.screen_contents()
     );
     assert!(
-        harness.is_running().expect("poll pager liveness"),
+        harness.is_running(),
         "pager exited\nscreen:\n{}",
         harness.screen_contents()
     );
