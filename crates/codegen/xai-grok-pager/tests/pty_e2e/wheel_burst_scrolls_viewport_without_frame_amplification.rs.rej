diff a/crates/codegen/xai-grok-pager/tests/pty_e2e/wheel_burst_scrolls_viewport_without_frame_amplification.rs b/crates/codegen/xai-grok-pager/tests/pty_e2e/wheel_burst_scrolls_viewport_without_frame_amplification.rs	(rejected hunks)
@@ -60,7 +60,7 @@ async fn wheel_burst_scrolls_viewport_without_frame_amplification() {
     harness.update(Duration::from_millis(600));
 
     assert!(
-        harness.is_running().expect("poll pager liveness"),
+        harness.is_running(),
         "pager exited during the wheel burst\nscreen:\n{}",
         harness.screen_contents()
     );
@@ -121,9 +121,8 @@ async fn wheel_burst_scrolls_viewport_without_frame_amplification() {
         BURST_INTERVAL,
     );
     harness.update(Duration::from_millis(300));
-    let running = harness.is_running().expect("poll pager liveness");
     assert!(
-        running && !harness.contains_text("panicked"),
+        harness.is_running() && !harness.contains_text("panicked"),
         "pager broke on a mixed-direction wheel sequence\nscreen:\n{}",
         harness.screen_contents()
     );
